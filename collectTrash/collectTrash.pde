

  if (jellyfish.length > 0 || bag.length() > 0) {
    length();

  }
  
  for (int i = 0; i <= jellyfish.length(); i++) {
    
    for (int giphy = 0; giphy <= bags.length(); giphy++) {
      int jellyfishbagDIST = dist(x, y, bag[giphy].x,bag[giphy].y;
      
      if (jellyfishbagDIST < 10) {
        bag.remove(giphy);
        }
    }
  }